name: CI-CD

on:
  # workflow_dispatch:
  push:
    branches:
      - main
  # pull_request:
    # branches:
      # - staging  # for main branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Code Checkout
      uses: actions/checkout@v4
